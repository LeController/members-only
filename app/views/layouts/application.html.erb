<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnly</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= link_to "HOME", root_path%>
    <br>
    <%= link_to "Edit profile", edit_user_registration_path, :method => :get if user_signed_in? %>
    <br>
    <%= link_to "Log in", new_user_session_path unless user_signed_in? %>
    <%= link_to "Log out", destroy_user_session_path, :method => :delete if user_signed_in? %>
    <br>
    <%= link_to "New Post", new_post_path %>

    <% flash.each do |type, msg| %>
      <div>
        <h2><%= msg %></h2>
      </div>
    <% end %>

    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if alert %>  
      <p class="alert"><%= alert %></p>
    <% end %>
    
    <%= yield %>
  </body>
</html>
